# 🔐 الملف الشامل لحساب المدير - T-REX Shop

## ✅ **تم الاختبار والتحقق:**

```
┌───────────────────────────────────────┐
│  حالة الحساب                          │
├───────────────────────────────────────┤
│  ✅ البريد: صحيح                     │
│  ✅ كلمة المرور: صحيحة               │
│  ✅ Hash: صحيح                      │
│  ✅ API: يعمل                       │
│  ✅ تسجيل الدخول: ناجح              │
└───────────────────────────────────────┘
```

---

## 📋 **بيانات تسجيل الدخول المؤكدة:**

### 1️⃣ **البريد الإلكتروني:**

```
mmm773769835@gmail.com
```

**المصدر:** ملف `server/.env` (السطر 6)  
**الحالة:** ✅ نشط ومجرّب  
**آخر اختبار:** نجح بنجاح!

---

### 2️⃣ **كلمة المرور:**

```
733770042Aa
```

**المصدر:** تم إنشاؤها بواسطة `set-admin-password.js`  
**الحالة:** ✅ صحيحة  
**Hash المخزن:** `$2b$10$.c7NixU1fQPbzP4TlYAAbuderltTBM1Z330O8S0W226zzKl6vyd..`

---

### 3️⃣ **اسم المستخدم (Username):**

```
owner
```

**المصدر:** ملف `server/.env` (السطر 4)

---

## 🔍 **التفاصيل الفنية الكاملة:**

### من ملف server/.env:

```env
# Admin Authentication
ADMIN_USER=owner
ADMIN_EMAIL=mmm773769835@gmail.com
ADMIN_HASH=$2b$10$.c7NixU1fQPbzP4TlYAAbuderltTBM1Z330O8S0W226zzKl6vyd..

# JWT Secret
JWT_SECRET=t-rex-shop-super-secret-jwt-key-2026-do-not-share

# Server Configuration
PORT=4001
NODE_ENV=development
```

---

### من كود السيرفر (server/index.js):

#### دالة تسجيل الدخول (السطر 535-595):

```javascript
app.post('/api/auth/admin-login', 
  validate([
    body('email')
      .notEmpty().withMessage('البريد الإلكتروني مطلوب')
      .isEmail().withMessage('البريد الإلكتروني غير صحيح'),
    body('password')
      .notEmpty().withMessage('كلمة المرور مطلوبة')
  ]),
  async (req, res) => {
    try {
      const { email, password } = req.body;
      
      // التحقق من أن البريد هو بريد المدير
      const adminEmail = process.env.ADMIN_EMAIL || 'admin@t-rex-shop.com';
      if (email !== adminEmail) {
        return res.status(401).json({
          error: 'unauthorized',
          message: 'البريد الإلكتروني أو كلمة المرور غير صحيحة'
        });
      }
      
      // التحقق من كلمة المرور
      const adminHash = process.env.ADMIN_HASH;
      if (!adminHash) {
        console.error('ADMIN_HASH not configured');
        return res.status(500).json({
          error: 'server_error',
          message: 'خطأ في إعدادات السيرفر'
        });
      }
      
      const passwordMatch = await bcrypt.compare(password, adminHash);
      if (!passwordMatch) {
        return res.status(401).json({
          error: 'unauthorized',
          message: 'البريد الإلكتروني أو كلمة المرور غير صحيحة'
        });
      }
      
      // إنشاء JWT token
      const token = jwt.sign(
        { 
          role: 'admin', 
          email: email,
          sub: process.env.ADMIN_USER || 'owner' 
        }, 
        JWT_SECRET,
        { expiresIn: '8h' }
      );
      
      console.log(`✅ Admin login successful: ${email}`);
      
      res.json({
        message: 'تم تسجيل الدخول بنجاح',
        token,
        user: {
          email,
          role: 'admin'
        }
      });
    } catch (error) {
      console.error('Admin login error:', error);
      res.status(500).json({
        error: 'server_error',
        message: 'حدث خطأ في السيرفر'
      });
    }
  }
);
```

---

## 🧪 **نتائج الاختبار:**

### اختبار 1: من PowerShell

```powershell
$body = @{
    email = "mmm773769835@gmail.com"
    password = "733770042Aa"
} | ConvertTo-Json

$result = Invoke-WebRequest -Uri "http://localhost:4001/api/auth/admin-login" `
  -Method POST `
  -ContentType "application/json" `
  -Body $body
```

**النتيجة:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYWRtaW4iLCJlbWFpbCI6Im1tbTc3Mzc2OTgzNUBnbWFpbC5jb20iLCJzdWIiOiJvd25lciIsImlhdCI6MTc3NDQ4MjQ1NSwiZXhwIjoxNzc0NTExMjU1fQ.-G-MQY9DtdL6cP4S4EzOfVxC0CD0zbyMR6JNRCuvd2k",
  "message": "تم تسجيل الدخول بنجاح",
  "user": {
    "email": "mmm773769835@gmail.com",
    "role": "admin"
  }
}
```

**الحالة:** ✅ **نجح!**

---

## 🔐 **تفاصيل الـ Token:**

### محتوى الـ JWT Token:

```json
{
  "role": "admin",
  "email": "mmm773769835@gmail.com",
  "sub": "owner",
  "iat": 1774482455,
  "exp": 1774511255
}
```

**المعاني:**
- `role`: "admin" - صلاحية مدير
- `email`: البريد الإلكتروني للمدير
- `sub`: "owner" - اسم المستخدم
- `iat`: وقت إصدار الـ Token
- `exp`: وقت انتهاء الصلاحية (8 ساعات)

---

## 📊 **مقارنة جميع الإصدارات:**

| الإصدار | البريد | كلمة المرور | Hash | الحالة |
|---------|--------|-------------|------|--------|
| **الأول** | admin@t-rex-shop.com | Admin@123456 | $2b$10$9H0kzt4lb7JuzoAx... | ❌ ملغى |
| **الثاني** | mmm712874799@gmail.com | Admin@123456 | $2b$10$9H0kzt4lb7JuzoAx... | ❌ ملغى |
| **الثالث** | mmm712874799@gmail.com | 733770042Aa | $2b$10$.c7NixU1fQPbzP4TlYAAbu... | ❌ ملغى |
| **الرابع** | mmm773769835@gmail.com | 733770042Aa | $2b$10$.c7NixU1fQPbzP4TlYAAbu... | ✅ نشط |

---

## ⚠️ **لماذا قد يظهر خطأ؟**

### 1️⃣ **خطأ في البريد:**

```
❌ mmm712874799@gmail.com (البريد القديم)
❌ mmm773769835@gmial.com (خطأ كتابي)
✅ mmm773769835@gmail.com (الصحيح)
```

**التحقق حرف بحرف:**
```
m-m-m-7-7-3-7-6-9-8-3-5 @ gmail . com
```

---

### 2️⃣ **خطأ في كلمة المرور:**

```
❌ 733770042aa (أحرف صغيرة فقط)
❌ 733770042AA (أحرف كبيرة فقط)
❌ 733770042 (ناقص أحرف)
✅ 733770042Aa (صحيح - A كبيرة، a صغيرة)
```

**التحقق:**
- الأرقام: `733770042` (9 أرقام)
- الأحرف: `Aa` (حرف كبير + حرف صغير)
- المجموع: `733770042Aa` (11 حرف)

---

### 3️⃣ **مسافات زائدة:**

```
❌ " mmm773769835@gmail.com " (مسافة قبل وبعد)
❌ "mmm773769835@gmail.com " (مسافة بعد)
✅ "mmm773769835@gmail.com" (بدون مسافات)
```

**نصيحة:** استخدم `.trim()` في الكود أو لا تضع مسافات عند الإدخال!

---

### 4️⃣ **السيرفر غير شغال:**

```bash
# تحقق من تشغيل السيرفر:
cd 'C:\Users\Abo Hamza\Desktop\T-REX_Shop_fu\server'
node index.js
```

**يجب أن ترى:**
```
🔐 T-REX Auth Server listening on port 4001
🏥 Health check: http://localhost:4001/api/health
✅ Admin login endpoint ready
```

---

### 5️⃣ **مشكلة في الاتصال:**

```bash
# اختبر الاتصال:
Invoke-WebRequest -Uri "http://localhost:4001/api/health"
```

**النتيجة المتوقعة:**
```json
{
  "status": "ok",
  "timestamp": "2026-03-25T...",
  "uptime": 78.77
}
```

---

## 🎯 **دليل الاستخدام الكامل:**

### من التطبيق (Expo Go):

```
1. افتح التطبيق
2. اذهب إلى "لوحة المدير"
3. أدخل:
   - البريد: mmm773769835@gmail.com
   - كلمة المرور: 733770042Aa
4. اضغط "دخول"
5. ✅ يجب أن ينجح!
```

---

### من المتصفح (API Test):

```javascript
// باستخدام Fetch API
fetch('http://localhost:4001/api/auth/admin-login', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    email: 'mmm773769835@gmail.com',
    password: '733770042Aa'
  })
})
.then(response => response.json())
.then(data => {
  console.log('✅ Success:', data);
  console.log('Token:', data.token);
})
.catch(error => {
  console.error('❌ Error:', error);
});
```

---

### من PowerShell:

```powershell
$body = @{
    email = "mmm773769835@gmail.com"
    password = "733770042Aa"
} | ConvertTo-Json

$result = Invoke-WebRequest -Uri "http://localhost:4001/api/auth/admin-login" `
  -Method POST `
  -ContentType "application/json" `
  -Body $body

# اعرض النتيجة
Write-Host $result.Content

# استخرج الـ Token
$response = $result.Content | ConvertFrom-Json
Write-Host "`nToken:" $response.token
```

---

## 💡 **نصائح ذهبية:**

### 1. **انسخ والصق:**
```
بدلاً من الكتابة، انسخ مباشرة من هذا الملف:
البريد: mmm773769835@gmail.com
كلمة المرور: 733770042Aa
```

### 2. **تحقق من كل حرف:**
```
اكتب الحروف واحداً تلو الآخر:
m-m-m-7-7-3-7-6-9-8-3-5 @ g-m-a-i-l . c-o-m
7-3-3-7-7-0-0-4-2-A-a
```

### 3. **لا مسافات:**
```
قبل الإدخال، تأكد من عدم وجود مسافات:
❌ " mmm773769835@gmail.com "
✅ "mmm773769835@gmail.com"
```

### 4. **الأحرف الكبيرة:**
```
Aa ← الأولى كبيرة، الثانية صغيرة
❌ aa ← خطأ
❌ AA ← خطأ
✅ Aa ← صحيح
```

### 5. **استخدم Copy-Paste:**
```
من هذا الملف، انسخ:
- السطر 1: البريد
- السطر 2: كلمة المرور
```

---

## 📝 **Checklist التحقق النهائي:**

```
□ 1. البريد: mmm773769835@gmail.com
□ 2. كلمة المرور: 733770042Aa
□ 3. لا مسافات قبل أو بعد
□ 4. A كبيرة و a صغيرة
□ 5. السيرفر شغال
□ 6. الاتصال سليم
□ 7. نفس الشبكة WiFi
□ 8. Firebase User موجود
```

---

## 🔗 **ملفات ذات صلة:**

| الملف | الوصف |
|-------|-------|
| [`server/.env`](./server/.env) | بيانات الاعتماد المشفرة |
| [`server/index.js`](./server/index.js) | كود المصادقة (السطر 535-595) |
| [`server/set-admin-password.js`](./server/set-admin-password.js) | سكريبت إنشاء كلمة المرور |
| [`firestore.rules`](./firestore.rules) | قواعد Firestore (السطر 12) |
| [`src/screens/AdminLoginScreen.tsx`](./src/screens/AdminLoginScreen.tsx) | واجهة تسجيل الدخول |

---

## 🎉 **النتيجة النهائية:**

```
┌───────────────────────────────────────┐
│  ✅ البيانات مؤكدة وصحيحة 100%       │
│  ✅ تم اختبارها بنجاح                 │
│  ✅ API يعمل                         │
│  ✅ Token تم إنشاؤه                  │
│  ✅ جاهز للاستخدام                   │
└───────────────────────────────────────┘
```

---

## 📞 **إذا استمر الخطأ:**

### أرسل هذه المعلومات:

```
1. نص الخطأ بالكامل: _______________
2. لقطة شاشة من الخطأ: _____________
3. طريقة الإدخال: (نسخ/كتابة) _____
4. هل السيرفر شغال؟ (نعم/لا) _______
5. نظام التشغيل: (Android/iOS/Web) _
```

---

**تم التحقق والتأكيد: البيانات صحيحة بنسبة 100%!** ✅

**التاريخ:** 2026-03-25  
**الحالة:** ✅ مؤكدة  
**آخر اختبار:** نجح بنجاح!  
**API:** http://localhost:4001/api/auth/admin-login
