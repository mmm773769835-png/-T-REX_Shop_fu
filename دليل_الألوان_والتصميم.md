# 🎨 دليل الألوان والتصميم الاحترافي - T-REX Shop

## 🌙 **الوضع الليلي الافتراضي**

### ✅ **تم تعيين الوضع الليلي كافتراضي:**

```typescript
// ThemeContext.tsx
isDarkMode: true, // ✅ الوضع الافتراضي
```

---

## 🎨 **لوحة الألوان الاحترافية:**

### الألوان الأساسية (Primary Colors):

```typescript
// الوضع الليلي (الافتراضي)
const darkTheme = {
  // الخلفيات
  background: '#0F172A',        // كحلي غامق - خلفية رئيسية
  surface: '#1E293B',           // كحلي متوسط - سطح البطاقات
  surfaceElevated: '#334155',   // كحلي فاتح - عناصر مرتفعة
  
  // الألوان الأساسية
  primary: '#3B82F6',           // أزرق ساطع - الإجراءات الأساسية
  primaryDark: '#1D4ED8',       // أزرق غامق - hover
  primaryLight: '#60A5FA',      // أزرق فاتح - focus
  
  secondary: '#8B5CF6',         // بنفسجي - إجراءات ثانوية
  accent: '#FBBF24',            // ذهبي - تمييز وإشعارات
  
  // حالات النجاح والخطأ
  success: '#10B981',           // أخضر - نجاح وتأكيد
  error: '#EF4444',             // أحمر - أخطاء وتحذيرات
  warning: '#F59E0B',           // برتقالي - تنبيهات
  info: '#3B82F6',              // أزرق - معلومات
  
  // النصوص
  textPrimary: '#F9FAFB',       // أبيض - نص أساسي
  textSecondary: '#D1D5DB',     // رمادي فاتح - نص ثانوي
  textDisabled: '#9CA3AF',      // رمادي - نص معطل
  
  // الحدود والفواصل
  border: '#374151',            // رمادي غامق - حدود
  divider: '#1F2937',           // رمادي أغمق - فواصل
  
  // الظلال
  shadow: 'rgba(0, 0, 0, 0.3)', // ظل داكن
  shadowLight: 'rgba(0, 0, 0, 0.2)', // ظل خفيف
};

// الوضع النهاري
const lightTheme = {
  // الخلفيات
  background: '#F8F9FA',        // رمادي فاتح جداً
  surface: '#FFFFFF',           // أبيض نقي
  surfaceElevated: '#F1F5F9',   // رمادي مزرق فاتح
  
  // الألوان الأساسية
  primary: '#2563EB',           // أزرق ملكي
  primaryDark: '#1E40AF',       // أزرق غامق
  primaryLight: '#3B82F6',      // أزرق فاتح
  
  secondary: '#7C3AED',         // بنفسجي غامق
  accent: '#F59E0B',            // ذهبي غامق
  
  // حالات النجاح والخطأ
  success: '#059669',           // أخضر غامق
  error: '#DC2626',             // أحمر غامق
  warning: '#D97706',           // برتقالي غامق
  info: '#2563EB',              // أزرق
  
  // النصوص
  textPrimary: '#111827',       // أسود فحمي
  textSecondary: '#6B7280',     // رمادي متوسط
  textDisabled: '#9CA3AF',      // رمادي فاتح
  
  // الحدود والفواصل
  border: '#E5E7EB',            // رمادي خفيف
  divider: '#F3F4F6',           // رمادي أفتح
  
  // الظلال
  shadow: 'rgba(0, 0, 0, 0.1)', // ظل خفيف
  shadowLight: 'rgba(0, 0, 0, 0.05)', // ظل خفيف جداً
};
```

---

## 🏞️ **التدرجات اللونية (Gradients):**

### تدرجات الخلفية:

```typescript
// تدرج الوضع الليلي
const darkGradient = {
  colors: ['#0F172A', '#1E293B', '#0F172A'],
  start: { x: 0, y: 0 },
  end: { x: 0, y: 1 },
};

// تدرج الوضع النهاري
const lightGradient = {
  colors: ['#F8F9FA', '#FFFFFF', '#F0F4FF'],
  start: { x: 0, y: 0 },
  end: { x: 0, y: 1 },
};

// تدرج الأزرار الأساسية
const primaryGradient = {
  colors: isDarkMode 
    ? ['#3B82F6', '#2563EB']  // ليلي
    : ['#2563EB', '#1D4ED8'], // نهاري
  start: { x: 0, y: 0 },
  end: { x: 1, y: 1 },
};

// تدرج البطاقات
const cardGradient = {
  colors: isDarkMode
    ? ['#1E293B', '#334155']   // ليلي
    : ['#FFFFFF', '#F8F9FA'],  // نهاري
  start: { x: 0, y: 0 },
  end: { x: 0, y: 1 },
};
```

---

## 🎭 **الظلال والارتفاعات (Shadows & Elevation):**

### مستويات الظل:

```typescript
//_shadowSm:轻度
shadowSmall: {
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 1 },
  shadowOpacity: isDarkMode ? 0.2 : 0.05,
  shadowRadius: 2,
  elevation: 1,
},

// ShadowMd: متوسطة
shadowMedium: {
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: isDarkMode ? 0.25 : 0.1,
  shadowRadius: 4,
  elevation: 3,
},

// ShadowLg: كبيرة
shadowLarge: {
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 4 },
  shadowOpacity: isDarkMode ? 0.3 : 0.15,
  shadowRadius: 8,
  elevation: 6,
},

// ShadowXl: ضخمة
shadowExtraLarge: {
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 8 },
  shadowOpacity: isDarkMode ? 0.35 : 0.2,
  shadowRadius: 16,
  elevation: 10,
},
```

---

## 🔘 **الحدود والزوايا (Borders & Radius):**

### أحجام الزوايا الدائرية:

```typescript
borderRadius: {
  none: 0,        // بدون تدوير
  sm: 4,          // صغير جداً
  md: 8,          // متوسط
  lg: 12,         // كبير
  xl: 16,         // أكبر
  '2xl': 20,     // كبير جداً
  '3xl': 24,     // ضخم
  full: 9999,    // دائري كامل
}
```

### سمك الحدود:

```typescript
borderWidth: {
  none: 0,
  thin: 1,
  medium: 2,
  thick: 4,
}
```

---

## 📱 **الأيقونات الاحترافية:**

### مكتبة Ionicons - جميع الأيقونات:

```typescript
// التنقل الأساسي
navigation: {
  home: 'home-outline',
  search: 'search-outline',
  menu: 'menu-outline',
  close: 'close-outline',
  back: 'arrow-back-outline',
  forward: 'arrow-forward-outline',
},

// المستخدم والحساب
user: {
  person: 'person-outline',
  personCircle: 'person-circle-outline',
  logIn: 'log-in-outline',
  logOut: 'log-out-outline',
  register: 'person-add-outline',
},

// التسوق
shopping: {
  cart: 'cart-outline',
  bag: 'bag-outline',
  basket: 'basket-outline',
  card: 'card-outline',
  cash: 'cash-outline',
  gift: 'gift-outline',
},

// المنتجات
products: {
  grid: 'grid-outline',
  list: 'list-outline',
  apps: 'apps-outline',
  cube: 'cube-outline',
  layer: 'layer-outline',
},

// التصنيفات
categories: {
  electronics: 'phone-portrait-outline',
  clothes: 'shirt-outline',
  home: 'home-outline',
  books: 'book-outline',
  sports: 'football-outline',
  beauty: 'sparkles-outline',
  toys: 'game-controller-outline',
  cars: 'car-outline',
  health: 'fitness-outline',
  grocery: 'cart-outline',
},

// الإجراءات
actions: {
  add: 'add-circle-outline',
  edit: 'create-outline',
  delete: 'trash-outline',
  save: 'save-outline',
  share: 'share-outline',
  download: 'download-outline',
  upload: 'cloud-upload-outline',
},

// التقييم والمفضلة
rating: {
  heart: 'heart-outline',
  heartFilled: 'heart',
  star: 'star-outline',
  starFilled: 'star',
  thumbsUp: 'thumbs-up-outline',
  thumbsDown: 'thumbs-down-outline',
},

// الاتصالات
communication: {
  mail: 'mail-outline',
  phone: 'call-outline',
  chat: 'chatbubble-outline',
  location: 'location-outline',
  globe: 'globe-outline',
},

// الإعدادات والنظام
system: {
  settings: 'settings-outline',
  help: 'help-circle-outline',
  info: 'information-circle-outline',
  checkmark: 'checkmark-circle-outline',
  warning: 'warning-outline',
  alert: 'alert-circle-outline',
  refresh: 'refresh-outline',
  reload: 'reload-outline',
},

// الوسائط المتعددة
media: {
  image: 'image-outline',
  images: 'images-outline',
  camera: 'camera-outline',
  video: 'videocam-outline',
  mic: 'mic-outline',
  musical: 'musical-notes-outline',
},

// الوقت والتاريخ
time: {
  time: 'time-outline',
  calendar: 'calendar-outline',
  today: 'calendar-number-outline',
},

// الطقس والطاقة
weather: {
  sunny: 'sunny-outline',
  moon: 'moon-outline',
  cloud: 'partly-sunny-outline',
  rain: 'rainy-outline',
  thunderstorm: 'thunderstorm-outline',
},

// الاتجاهات
directions: {
  up: 'arrow-up-outline',
  down: 'arrow-down-outline',
  left: 'arrow-left-outline',
  right: 'arrow-right-outline',
  upRight: 'arrow-up-right-outline',
  downLeft: 'arrow-down-left-outline',
},

// أخرى
misc: {
  expand: 'expand-outline',
  collapse: 'collapse-outline',
  eye: 'eye-outline',
  eyeOff: 'eye-off-outline',
  lock: 'lock-closed-outline',
  unlock: 'lock-open-outline',
  key: 'key-outline',
  fingerprint: 'finger-print-outline',
  barcode: 'barcode-outline',
  qrCode: 'qr-code-outline',
},
```

---

## 📏 **المسافات الموحدة (Spacing System):**

```typescript
spacing: {
  0: 0,
  xs: 4,    // 0.25rem
  sm: 8,    // 0.5rem
  md: 12,   // 0.75rem
  lg: 16,   // 1rem
  xl: 20,   // 1.25rem
  '2xl': 24, // 1.5rem
  '3xl': 32, // 2rem
  '4xl': 40, // 2.5rem
  '5xl': 48, // 3rem
  '6xl': 64, // 4rem
}
```

---

## 🔤 **أحجام الخطوط (Typography Scale):**

```typescript
fontSize: {
  xs: 10,     // صغير جداً - تسميات
  sm: 12,     // صغير - نص مساعد
  md: 14,     // متوسط - نص ثانوي
  lg: 16,     // كبير - نص أساسي
  xl: 18,     // أكبر - عناوين فرعية
  '2xl': 20,  // كبير جداً - عناوين
  '3xl': 24,  // ضخم - عناوين رئيسية
  '4xl': 32,  // أكبر ضخمة - عناصف الصفحة
  '5xl': 48,  // عملاقة - عروض خاصة
},

fontWeight: {
  thin: '300',
  regular: '400',
  medium: '500',
  semibold: '600',
  bold: '700',
  extrabold: '800',
},

lineHeight: {
  none: 1,
  tight: 1.25,
  normal: 1.5,
  relaxed: 1.75,
  loose: 2,
},
```

---

## 🎯 **تطبيق عملي - مثال بطاقة منتج:**

```typescript
const ProductCard = ({ product }) => {
  const { isDarkMode } = useContext(ThemeContext);
  
  return (
    <View style={[
      styles.card,
      {
        backgroundColor: isDarkMode ? colors.surface : '#FFFFFF',
        borderColor: isDarkMode ? colors.border : colors.border,
      }
    ]}>
      {/* صورة المنتج */}
      <Image 
        source={{ uri: product.image }} 
        style={styles.image}
      />
      
      {/* شارة التصنيف */}
      <View style={[
        styles.badge,
        { backgroundColor: colors.primary }
      ]}>
        <Ionicons name="pricetag" size={12} color="#fff" />
        <Text style={styles.badgeText}>{product.category}</Text>
      </View>
      
      {/* معلومات المنتج */}
      <View style={styles.content}>
        <Text style={[
          styles.title,
          { color: isDarkMode ? colors.textPrimary : colors.textPrimary }
        ]}>
          {product.name}
        </Text>
        
        <Text style={[
          styles.description,
          { color: isDarkMode ? colors.textSecondary : colors.textSecondary }
        ]}>
          {product.description}
        </Text>
        
        <View style={styles.footer}>
          <Text style={[
            styles.price,
            { color: isDarkMode ? colors.success : colors.success }
          ]}>
            ${product.price}
          </Text>
          
          <TouchableOpacity style={[
            styles.addButton,
            { backgroundColor: colors.primary }
          ]}>
            <Ionicons name="add" size={20} color="#fff" />
          </TouchableOpacity>
        </View>
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  card: {
    borderRadius: 16,
    borderWidth: 1,
    overflow: 'hidden',
    ...shadowMedium,
  },
  image: {
    width: '100%',
    height: 200,
    resizeMode: 'cover',
  },
  badge: {
    position: 'absolute',
    top: 12,
    left: 12,
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingVertical: 6,
    borderRadius: 20,
    gap: 4,
  },
  badgeText: {
    color: '#fff',
    fontSize: 12,
    fontWeight: '600',
  },
  content: {
    padding: 16,
    gap: 8,
  },
  title: {
    fontSize: 18,
    fontWeight: '700',
  },
  description: {
    fontSize: 14,
    lineHeight: 20,
  },
  footer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: 8,
  },
  price: {
    fontSize: 20,
    fontWeight: '700',
  },
  addButton: {
    width: 40,
    height: 40,
    borderRadius: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
```

---

## ✅ **ملخص التحسينات:**

```
┌───────────────────────────────────────┐
│  التحسينات المطبقة                    │
├───────────────────────────────────────┤
│  ✅ الوضع الليلي: افتراضي            │
│  ✅ ألوان احترافية: متناسقة          │
│  ✅ تدرجات لونية: جميلة             │
│  ✅ ظلال واحداثات: طبيعية           │
│  ✅ أيقونات Shاملة: واضحة            │
│  ✅ خطوط موحدة: مقروئية             │
│  ✅ مسافات منظمة: متناسقة           │
│  ✅ زوايا دائرية: ناعمة             │
└───────────────────────────────────────┘
```

---

**دليل التصميم الاحترافي الكامل!** 🎨✨
