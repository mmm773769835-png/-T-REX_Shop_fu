rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Development-only rules. DO NOT deploy to production.
    // These rules require authentication but allow all authenticated users access.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}